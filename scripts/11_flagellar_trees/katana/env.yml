# Katana environment for the flagellar tree run (created by 00_setup_env.pbs
# into <repo>/.katana_env). Versions pinned so reruns are comparable.
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.11
  - iqtree=2.4.0
  - mafft=7.526
  - trimal=1.5.0
  - numpy
  - ete3=3.1.3
